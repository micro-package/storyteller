export enum LoggingLevel {
  error = "error",
  descent = "descent",
  ascent = "ascent",
  plugin = "plugin",
  debug = "debug",
}

export enum LoggingSource {
  step = "step",
  section = "section",
  scenario = "scenario",
  express = "express",
  axios = "axios",
}
