export enum LoggingLevel {
  error = "error",
  descent = "descent",
  ascent = "ascent",
  plugin = "plugin",
  debug = "debug",
}

export enum LoggingSource {
  step = "step",
  section = "section",
  story = "story",
  express = "express",
  axios = "axios",
}
